steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/sports-server-prod', '.']

  # Push image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/sports-server-prod']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      [
        'run', 'deploy', 'sports-server-prod',
        '--image=gcr.io/$PROJECT_ID/sports-server-prod',
        '--region=us-central1',
        '--platform=managed',
        '--allow-unauthenticated'
      ]

# Define the image that was built
images:
  - 'gcr.io/$PROJECT_ID/sports-server-prod'
